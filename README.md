# university_databricks_overview

This repository is used for the **Databricks University Alliance Program** and contains example assets and code that support university data events and teaching materials.

## Where the code lives

Code examples, notebooks, and utilities are in **`src/`**:

- `src/uva_university_alliance/` – University Alliance demo notebooks (setup, load data, relationships) and instructions

## Where the data lives

Datasets used by the examples are in **`data/`**:

- CSV files for the World Cup demo (e.g. `matches.csv`, `players.csv`, `teams.csv`)
- See `data/README.md` for usage notes
